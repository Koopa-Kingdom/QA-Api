var routes = require('./index');
var router = require('express').Router();

router.get('/messages', routes.);

router.post('/messages', );

router.get('/users', );

router.post('/users', );


module.exports = router;

